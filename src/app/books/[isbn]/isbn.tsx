"use client"
import React from "react";
import Layout from "@/components/Layout"

const isbn = () => {
    return(
        <Layout>
            <h1>Book isbn</h1>
        </Layout>
    );
}

export default isbn;